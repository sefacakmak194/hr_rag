{
  "name": "private-hr-rag",
  "version": "1.0.0",
  "private": true,
  "license": "UNLICENSED",
  "type": "module",
  "description": "Private Enterprise HR & Policy Local RAG Assistant — 100% air-gapped, Microsoft Foundry Local",
  "scripts": {
    "setup": "npm install --prefix server && npm install --prefix client",
    "ingest": "npm run ingest --prefix server",
    "test:rag": "npm run test:rag --prefix server",
    "server": "npm start --prefix server",
    "client": "npm run dev --prefix client",
    "build:exe": "npm run build --prefix client && npm run build:exe --prefix server",
    "pdf": "npm run pdf --prefix server",
    "test": "npm test --prefix server",
    "eval": "npm run eval --prefix server",
    "compare": "npm run compare --prefix server"
  }
}
